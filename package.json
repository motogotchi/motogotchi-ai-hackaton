{
  "name": "motobot",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "prebuild": "pnpm -r prebuild",
    "build": "pnpm -r build",
    "deploy": "pnpm -r build && dfx deploy"
  }
}
